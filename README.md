# Project Context Library

A concrete OKF Library application that preserves versioned project understanding across Agent sessions and subagents.

This repository depends on the generic OKF Library Framework. The generic OKF repositories do not depend on this application.
